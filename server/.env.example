PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/samity_cms
CORS_ORIGIN=http://localhost:5173

# Lowercase names work too (e.g. some Vercel setups): mongodb_uri, cors_origin, port
# Optional: load realistic demo members, deposits, investments, ledger
# npm run seed:demo
# SEED_DEMO_FORCE=1 npm run seed:demo   # replace existing demo samity data
# seed_demo_force=1 npm run seed:demo   # lowercase ok
